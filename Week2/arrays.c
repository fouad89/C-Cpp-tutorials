#include <stdio.h>
#include <stdlib.h>

int main()
{
    double embtyArray[10]; // creating an empty array with 10 potential elements
    int luckyNums[] = {5, 6, 7, 9, 19}; // [] tells C that its an array

    printf("%d", luckyNums[0]);
    return 0;
}